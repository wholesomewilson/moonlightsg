<%= form_for(resource, as: resource_name, remote: true, url: registration_path(resource_name), html: { method: :put, id:"notification_form" }) do |f| %>
  <%= f.hidden_field :endpoint %>
  <%= f.hidden_field :p256dh %>
  <%= f.hidden_field :auth %>
<% end %>
